"""
Демонстрация работы индикаторов пропущенных значений

Показывает ТРИ сценария:
1. Нет пропусков → индикатор = константа (все 0)
2. Все пропуски → индикатор = константа (все 1)
3. Частичные пропуски → индикатор имеет вариацию (0 и 1) ✅ РАБОТАЕТ
"""

import pandas as pd
import numpy as np

# Симуляция данных
np.random.seed(42)

def analyze_scenario(data, scenario_name):
    """Анализ сценария с индикаторами"""
    print(f"\n{'='*70}")
    print(f"СЦЕНАРИЙ: {scenario_name}")
    print(f"{'='*70}")
    
    # Создаем индикатор пропусков
    data['q1_missing'] = data['q1'].isna().astype(int)
    
    # Статистика пропусков
    missing_count = data['q1_missing'].sum()
    total_count = len(data)
    missing_pct = (missing_count / total_count) * 100
    
    print(f"Всего записей: {total_count}")
    print(f"Пропусков в q1: {missing_count} ({missing_pct:.1f}%)")
    print(f"q1_missing значения: {data['q1_missing'].value_counts().to_dict()}")
    
    # Проверка на константность
    std_q1_missing = data['q1_missing'].std()
    is_constant = std_q1_missing == 0
    
    print(f"\nСтандартное отклонение q1_missing: {std_q1_missing:.4f}")
    
    if is_constant:
        print("❌ ИНДИКАТОР КОНСТАНТЕН → будет исключен из модели")
        print("   Веса для q1_missing НЕ будут рассчитаны")
    else:
        print("✅ ИНДИКАТОР ИМЕЕТ ВАРИАЦИЮ → может быть включен в модель")
        print("   Веса для q1_missing БУДУТ рассчитаны")
        
        # Импутация и расчет корреляции
        data_imputed = data.copy()
        data_imputed['q1'] = data_imputed['q1'].fillna(data_imputed['q1'].mean())
        
        # Корреляция индикатора с зависимой переменной
        corr = data_imputed[['q1_missing', 'y']].corr().iloc[0, 1]
        print(f"   Корреляция q1_missing с y: {corr:.4f}")
        
        if abs(corr) > 0.1:
            print(f"   → Сам факт пропуска ЗНАЧИМ для предсказания!")
        else:
            print(f"   → Факт пропуска малозначим")
    
    return not is_constant


# СЦЕНАРИЙ 1: НЕТ ПРОПУСКОВ
print("\n" + "🔷"*35)
print("ТЕОРИЯ: Когда индикаторы пропусков НЕ РАБОТАЮТ")
print("🔷"*35)

data1 = pd.DataFrame({
    'q1': np.random.normal(5, 1, 100),  # Нет пропусков
    'q2': np.random.normal(5, 1, 100),
    'y': np.random.normal(10, 2, 100)
})
analyze_scenario(data1, "НЕТ ПРОПУСКОВ в q1")


# СЦЕНАРИЙ 2: ВСЕ ПРОПУСКИ
data2 = pd.DataFrame({
    'q1': [np.nan] * 100,  # Все пропуски
    'q2': np.random.normal(5, 1, 100),
    'y': np.random.normal(10, 2, 100)
})
analyze_scenario(data2, "ВСЕ ЗНАЧЕНИЯ ПРОПУЩЕНЫ в q1")


# СЦЕНАРИЙ 3: ЧАСТИЧНЫЕ ПРОПУСКИ (ИДЕАЛЬНЫЙ СЛУЧАЙ)
print("\n" + "🔶"*35)
print("ПРАКТИКА: Когда индикаторы пропусков РАБОТАЮТ")
print("🔶"*35)

data3 = pd.DataFrame({
    'q1': np.random.normal(5, 1, 100),
    'q2': np.random.normal(5, 1, 100),
    'y': np.random.normal(10, 2, 100)
})
# 30% случайных пропусков
missing_mask = np.random.choice([True, False], size=100, p=[0.3, 0.7])
data3.loc[missing_mask, 'q1'] = np.nan

# Делаем пропуски неслучайными: где y низкое, больше пропусков
# Это симулирует ситуацию "те, кто недоволен, не отвечают"
low_satisfaction = data3['y'] < data3['y'].median()
data3.loc[low_satisfaction & missing_mask, 'q1'] = np.nan

analyze_scenario(data3, "ЧАСТИЧНЫЕ ПРОПУСКИ в q1 (30%)")


# РЕАЛЬНОСТЬ: Что происходит в вашем коде
print("\n" + "🔴"*35)
print("ЧТО ПРОИСХОДИТ В ВАШЕМ КОДЕ")
print("🔴"*35)

print("""
В функции hybrid_imputation():
1. Создаются индикаторы для ВСЕХ независимых переменных
2. Индикаторы добавляются в extended_vars

В функции calculate_weights():
3. Проверяется стандартное отклонение ВСЕХ переменных
4. Переменные с std=0 ИСКЛЮЧАЮТСЯ из модели
5. Расчет весов только для оставшихся переменных

РЕЗУЛЬТАТ:
- Если пропусков нет (код 99 обработан) → индикатор = все 0 → ИСКЛЮЧЕН
- Если пропусков немного → индикатор = почти все 0 → ИСКЛЮЧЕН  
- Если пропусков много → индикатор = почти все 1 → ИСКЛЮЧЕН
- Если все пропуски → индикатор = все 1 → ИСКЛЮЧЕН

✅ Индикатор ВКЛЮЧАЕТСЯ только когда:
   - В подгруппе есть И пропуски И непропуски
   - Доля пропусков от ~10% до ~90%
   - Индикатор имеет достаточную вариацию
""")

print("\n" + "💡"*35)
print("ВЫВОД")
print("💡"*35)

print("""
Расширенные переменные (индикаторы пропусков) ТЕОРЕТИЧЕСКИ полезны:
✅ Они могут выявить, если сам факт пропуска является предиктором
✅ Пример: "Те, кто не ответил на вопрос NPS, недовольны"

НО на ПРАКТИКЕ в вашем коде:
❌ Код 99 ("Затрудняюсь ответить") заменяется на NaN
❌ После импутации в большинстве подгрупп:
   - Либо нет пропусков (индикатор = все 0)
   - Либо очень мало/много пропусков (индикатор почти константен)
❌ Константные индикаторы автоматически исключаются
❌ В итоговых результатах их нет

РЕКОМЕНДАЦИЯ:
1. Если в ваших данных код 99 встречается редко → индикаторы бесполезны
2. Если код 99 систематически связан с результатом → индикаторы ценны
3. Проверьте реальные данные: есть ли вариация в индикаторах?
4. Если нет - упростите код, удалив индикаторы
""")

