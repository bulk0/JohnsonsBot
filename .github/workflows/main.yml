name: 🚀 Деплой в Yandex Cloud

# Запуск при пуше в ветку main
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    name: 🔧 Выполнить развертывание
    runs-on: ubuntu-latest

    steps:
      # 1. Получить код из репозитория
      - name: 📦 Получить код
        uses: actions/checkout@v4

      # 2. Установить YC CLI
      - name: 🔧 Установить YC CLI
        run: |
          curl -sSL https://storage.yandexcloud.net/yandexcloud-yc/install.sh | bash
          echo "$HOME/.yc/bin" >> $GITHUB_PATH

      # 3. Настроить профиль Yandex Cloud с помощью сервисного аккаунта
      - name: 🔐 Настроить доступ к Yandex Cloud
        uses: yandex-cloud/actions/configure@v1
        with:
          cloud-id: ${{ secrets.YC_CLOUD_ID }}
          folder-id: ${{ secrets.YC_FOLDER_ID }}
          key-id: ${{ secrets.YC_SERVICE_ACCOUNT_KEY_ID }}
          service-account-key: ${{ secrets.YC_SERVICE_ACCOUNT_SECRET_KEY }}

      # 4. Настроить доступ к Container Registry
      - name: 🐳 Настроить Container Registry
        run: |
          yc container registry configure-docker

      # 5. Собрать и загрузить Docker образ
      - name: 📦 Собрать и загрузить Docker образ
        env:
          CR_REGISTRY: ${{ secrets.YC_REGISTRY }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t cr.yandex/$CR_REGISTRY/johnsons-bot:$IMAGE_TAG .
          docker push cr.yandex/$CR_REGISTRY/johnsons-bot:$IMAGE_TAG

      # 6. Развернуть в Serverless Containers
      - name: 🚀 Развернуть в Serverless Containers
        run: |
          yc serverless container revision deploy \
            --container-name johnsons-bot \
            --image cr.yandex/${{ secrets.YC_REGISTRY }}/johnsons-bot:${{ github.sha }} \
            --cores 1 \
            --memory 512M \
            --concurrency 1 \
            --execution-timeout 30s \
            --service-account-id ${{ secrets.YC_SERVICE_ACCOUNT_ID }}